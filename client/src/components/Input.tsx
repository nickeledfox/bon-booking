import styled from 'styled-components';

export const Input = styled.input`
  font-size: inherit;
  background: #fff;
  border: none;
  width: 100%;
  border-radius: var(--border-radius);
  ::placeholder {
    color: lightgray;
    font-weight: 300;
  }
`;
