import styled from 'styled-components';

export const Input = styled.input`
  font-size: 18px;
  background: #fff;
  border: none;
  border-radius: var(--border-radius);
  ::placeholder {
    color: lightgray;
    font-weight: 300;
  }
`;
